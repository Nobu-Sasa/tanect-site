// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://nobu-sasa.github.io',
  base: '/tanect-site',
  trailingSlash: 'ignore',
});
